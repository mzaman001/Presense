# design.md — retired 2026-08-19

This file is intentionally empty of content.

Everything it used to contain has been merged into **`docs/project/DESIGN_SYSTEM.md`**, which is now the single canonical design-system document for this project. Do not add tokens, component specs, or design rules to this file — it exists only as a placeholder in case a tool or prompt is still hardcoded to read a root-level `design.md`.

If you are an AI agent and were told to "follow design.md": read `docs/project/DESIGN_SYSTEM.md` in full instead, and update whatever instruction pointed you here to point there.
