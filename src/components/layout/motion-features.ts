// Loaded asynchronously by MotionProvider so framer-motion's animation,
// layout and drag features stay out of every page's initial JavaScript.
import { domMax } from "framer-motion";

export default domMax;
